import browser from './browser';
import karmaConfig from './karmaConfig';
import libs from './libs';
import webpackConfig from './webpackConfig';
import nightwatchConfig from './nightwatchConfig';

export {
  // Browser tests setup utils
  browser,
  // Karma config helper
  karmaConfig,
  // Assertion libs
  libs,
  // Webpack config for tests
  webpackConfig,
  // Nightwatch config helper
  nightwatchConfig,
};
