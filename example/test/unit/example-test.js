import { expect } from '../support/config';

describe('Unit Test', () => {
  describe('example', () => {
    it('works', () => {
      expect(1).to.be.equal(1);
      expect(true).to.be.ok;
    });
  });
});
